
clear();

u_wf = add_unit( "weatherreader" );
u_sc = add_unit( "solarcollector" );

connect( u_wf, "xyz_output", u_sc, "yzg_input", -0.12, -1 );

setup( 1, 8760, 0.5, 100 );

simulate( 1, 8760, 0.5, 100 );